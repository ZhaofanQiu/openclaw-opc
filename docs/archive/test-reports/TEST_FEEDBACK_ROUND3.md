# OpenClaw OPC v0.3.0-beta 测试问题汇总

**测试时间**: 2026-03-22  
**测试人员**: 产品负责人  
**测试环境**: https://5fe0b0f7.r19.cpolar.top

---

## 问题清单

### P0 - 阻塞问题 (必须修复)

#### #1 Partner 员工缺失导致功能异常
**状态**: 🔴 待修复  
**影响**: Partner 相关功能完全不可用

**问题描述**:
- 数据库中没有 Partner 员工
- 无法唤醒 Partner
- Partner 相关功能（任务协助、管理建议等）不可用

**复现步骤**:
1. 进入 Dashboard
2. 尝试与 Partner 对话 → 无响应
3. 尝试创建需要 Partner 协助的任务 → 失败

**建议方案**:
- 选项 A: 系统初始化时自动创建默认 Partner
- 选项 B: 首次使用时引导用户创建/绑定 Partner
- 选项 C: 检查到无 Partner 时显示创建引导界面

**决策**: 采用 **选项 C** - 无侵入式引导，用户自主选择

---

### P1 - 高优先级

#### #2 公司预算管理功能缺失
**状态**: 🟡 待设计  
**影响**: 预算无法调整，不符合实际业务场景

**问题描述**:
- 公司总预算无法修改
- 总预算应该是月度预算而非一次性预算
- 缺乏预算增加/调整功能

**建议方案**:
```
1. 添加预算管理页面/弹窗
2. 总预算 → 改为月度预算
3. 支持手动增加预算（如追加投资）
4. 每月自动重置预算额度
5. 显示预算使用历史图表
```

**UI 建议**:
- 在 Dashboard 添加"预算管理"入口
- 弹窗包含：当前月度预算、本月已用、修改预算按钮

---

#### #3 多语言支持不完整
**状态**: 🟡 进行中  
**影响**: 英文用户/开发者体验差

**问题描述**:
- 界面多处未翻译或翻译不当
- 某些动态内容（错误消息、状态文本）未国际化

**当前完成度**: ~70%

**待完善区域**:
- [ ] 错误提示消息
- [ ] 动态生成的状态文本
- [ ] 日期时间格式
- [ ] 数字/货币格式
- [ ] 部分模态框标题

**建议方案**:
1. 全面审计所有 UI 文本
2. 添加缺失的 `data-i18n` 属性
3. 补充 zh.json / en.json
4. 添加语言切换器到设置页面

---

#### #4 随机头像生成功能报错
**状态**: 🔴 待修复  
**影响**: 员工头像自定义功能不可用

**问题描述**:
- 点击"系统生成-随机生成"时报错："生成失败，请稍后重试"
- 可能原因：
  - API 调用失败
  - 返回格式解析错误
  - 网络超时

**排查步骤**:
1. 检查浏览器控制台网络请求
2. 检查后端 `/api/avatars/generate` 接口
3. 检查第三方头像生成服务状态

**临时方案**: 允许用户上传本地图片作为头像

---

### P2 - 中优先级

#### #5 Agent 绑定流程重复
**状态**: 🟡 待优化  
**影响**: 用户体验困惑

**问题描述**:
- 未绑定 Agent 的员工有"创建 Agent"和"绑定 Agent"两个选项
- "绑定 Agent"弹窗内又有"创建 Agent"选项
- 逻辑重复，用户困惑

**当前流程**:
```
员工卡片
  ├── 创建 Agent
  └── 绑定 Agent
        └── 弹窗内有"创建 Agent"
```

**建议方案**:
```
简化流程：
员工卡片
  └── 绑定 Agent
        ├── 选择现有 Agent (列表)
        └── 创建新 Agent (表单)
```

**具体改动**:
1. 移除员工卡片的"创建 Agent"按钮
2. 绑定弹窗使用 Tab 切换：选择现有 / 创建新
3. 创建新 Agent 后直接完成绑定

---

#### #6 未绑定 Agent 员工视觉状态不明确
**状态**: 🟡 待设计  
**影响**: 用户无法直观判断员工可用状态

**问题描述**:
- 未绑定 Agent 的员工也是绿色呼吸特效
- 视觉上与正常工作员工无区别
- 用户无法快速识别哪些员工"无法工作"

**当前状态**:
| 状态 | 视觉效果 |
|------|----------|
| 已绑定+在线 | 绿色呼吸 |
| 已绑定+离线 | 灰色 |
| 未绑定 | 绿色呼吸 ❌ |

**建议方案**:
```
未绑定 Agent 员工：
- 头像：灰度/半透明
- 边框：虚线或灰色
- 呼吸特效：停止或改为灰色闪烁
- 添加"未绑定"标签覆盖在头像上
```

**视觉设计**:
```css
/* 未绑定状态 */
.employee-card.unbound {
  opacity: 0.7;
  filter: grayscale(80%);
}
.employee-card.unbound .avatar {
  border: 2px dashed #999;
}
.employee-card.unbound::after {
  content: "未绑定";
  position: absolute;
  background: rgba(0,0,0,0.6);
  color: white;
  padding: 2px 8px;
  border-radius: 4px;
}
```

---

## 已处理问题

### ✅ 数据重置
**操作**: 删除所有员工和任务数据  
**时间**: 2026-03-22  
**状态**: 已完成，可从头测试全流程

---

## 修复计划

| 优先级 | 问题 | 预计工时 | 负责人 |
|--------|------|----------|--------|
| P0 | #1 Partner 引导 | 4h | Kimi Claw |
| P1 | #4 头像生成修复 | 2h | Kimi Claw |
| P1 | #3 多语言完善 | 6h | Kimi Claw |
| P1 | #2 预算管理 | 8h | Kimi Claw |
| P2 | #5 绑定流程优化 | 3h | Kimi Claw |
| P2 | #6 视觉状态 | 2h | Kimi Claw |

**总计**: 约 25 工时

---

## 决策记录

### 关于 Partner 默认创建
**讨论**: 是否系统强制创建默认 Partner？

**结论**: 不强制创建，采用引导式方案
- 尊重用户选择权
- 避免创建无用的默认数据
- 引导用户理解 Partner 角色后再创建

**实现方式**:
- Dashboard 检测无 Partner 时显示引导卡片
- 首次创建普通员工时提示"是否同时创建 Partner 助理？"
- Partner 管理页面显示创建入口

### 关于月度预算
**讨论**: 总预算 vs 月度预算？

**结论**: 改为月度预算 + 手动追加模式
- 更符合 SaaS 产品计费习惯
- 允许突发情况追加预算
- 保留历史预算使用记录

---

## 运维问题

### #7 后台服务频繁停止
**状态**: ✅ 已修复  
**影响**: 测试过程中服务多次中断，影响测试体验

**问题描述**:
- OPC Backend 服务 (`uvicorn`) 在使用 `&` 后台运行时频繁停止
- 每次测试中断后需要手动重启服务
- Cpolar 隧道断开需要重新获取 URL

**根本原因**:
- `nohup` + `&` 后台运行在某些环境中进程仍会被父 shell 终止
- Cpolar 免费版 URL 在隧道重启后会变化

**解决方案**:

#### 1. 启动 OPC Backend (使用 setsid 替代 nohup)
```bash
cd /root/.openclaw/workspace/openclaw-opc/backend
# 错误方式 ❌: 进程可能随 shell 断开而停止
nohup python3 -m uvicorn src.main:app --host 127.0.0.1 --port 8080 &

# 正确方式 ✅: 进程完全脱离当前会话
setsid python3 -m uvicorn src.main:app --host 127.0.0.1 --port 8080 >> app.log 2>&1 &
```

#### 2. 启动 Cpolar 隧道
```bash
# 在确认本地服务正常后再启动
curl http://127.0.0.1:8080/health

# 启动隧道
cpolar http 8080

# 获取公网 URL
curl -s http://127.0.0.1:4040/http/in | grep -oE "https://[a-zA-Z0-9_-]+\.cpolar[^\"]*" | head -1
```

#### 3. 服务监控脚本 (可选)
```bash
#!/bin/bash
# save as: /root/.openclaw/workspace/openclaw-opc/backend/monitor.sh

while true; do
    if ! curl -s http://127.0.0.1:8080/health > /dev/null; then
        echo "$(date): OPC 服务异常，正在重启..." >> service_monitor.log
        setsid python3 -m uvicorn src.main:app --host 127.0.0.1 --port 8080 >> app.log 2>&1 &
        sleep 5
    fi
    sleep 30
done
```

**启动顺序**:
1. 先启动 OPC Backend (`setsid` 方式)
2. 验证 `curl http://127.0.0.1:8080/health` 返回正常
3. 再启动 Cpolar 隧道
4. 获取并记录新的公网 URL

**关键检查点**:
```bash
# 检查 OPC 服务
curl http://127.0.0.1:8080/health

# 检查 Cpolar 状态
curl http://127.0.0.1:4040/http/in | grep PublicUrl

# 检查端口监听
netstat -tlnp | grep -E "8080|4040"
```

---

## 附录

### 测试环境信息
```
URL: https://5fe0b0f7.r19.cpolar.top
版本: v0.3.0-beta
数据库: SQLite (已清空重置)
```

### 待验证功能
- [ ] Partner 引导流程
- [ ] 预算管理界面
- [ ] 多语言完整切换
- [ ] 头像生成功能
- [ ] 简化绑定流程
- [ ] 员工状态视觉区分
